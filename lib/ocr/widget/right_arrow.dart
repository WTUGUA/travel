import 'package:flutter/material.dart';

class DefaultRightArrow extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Icon(Icons.arrow_forward_ios, size: 14.0, color: Colors.grey);
  }
}
